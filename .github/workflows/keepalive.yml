name: Keep Bot Alive

on:
  schedule:
    - cron: '0 */6 * * *'  # runs every 6 hours to keep the repo active

jobs:
  keepalive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: gautamkrishnar/keepalive-workflow@v2
